generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model match {
  id         String      @id(map: "matches_pkey") @db.Uuid
  teams      String[]    @db.Uuid
  winner     String?     @db.Uuid
  tournament String?     @db.Uuid
  start_time DateTime?   @db.Date
  scores     Int[]
  match_type match_type?
}

model profile {
  id      String   @id(map: "profiles_pkey") @db.Uuid
  history Json[]
  cards   String[] @db.Uuid
  record  Json[]
  elo     Float[]  @db.Real
}

model team {
  id      String   @id(map: "teams_pkey") @db.Uuid
  name    String?
  players String[] @db.Uuid
}

model tournament {
  id              String            @id(map: "tournaments_pkey") @db.Uuid
  capacity        Int?
  players         String[]          @db.Uuid
  matches         String[]          @db.Uuid
  start_date      DateTime?         @db.Date
  end_date        DateTime?         @db.Date
  tournament_type tournament_types?
  status          status?
}

enum match_type {
  singles
  doubles
}

enum status {
  upcoming
  ongoing
  finished
}

enum status_enum {
  upcoming
  ongoing
  finished
}

enum tournament_types {
  single_elimination
  double_elimination
  round_robin
}
